# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Actual environment files (do NOT commit these)
.env
.env.local
.env.production
.env.staging
.env.development
# Keep the template tracked (do NOT ignore)
!.env.template

# All mutable state lives under data/ (mounted volume in production)
# Old locations kept here during transition period
/data/
logs/
workspace/
memory_db/
messages/
shared_knowledge/